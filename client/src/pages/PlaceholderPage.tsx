import { useLocation } from 'react-router-dom'

export function PlaceholderPage() { const { pathname } = useLocation(); const title = pathname.slice(1).split('-').map((word) => word.charAt(0).toUpperCase() + word.slice(1)).join(' '); return <div className="border border-[var(--silver)] bg-white p-8"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[var(--royal-blue)]">RRVMS workspace</p><h1 className="display mt-3 text-4xl font-bold text-[var(--royal-blue)]">{title}</h1><p className="mt-4 max-w-xl text-sm leading-6 text-[var(--muted)]">This area is reserved for the next RRVMS implementation phase. The navigation and layout are ready for its workflow.</p><span className="mt-7 inline-block rounded-[4px] border border-[var(--silver)] bg-[var(--surface)] px-3 py-1 text-xs font-semibold uppercase tracking-wide text-[var(--muted)]">Coming soon</span></div> }
